# Flurry / Fluffy — Target Prompt（后续编程指导）

> **用途**：本文档是后续一切功能实现、重构与 Code Review 的**目标约束说明书**。  
> 实现任何功能前先对照本文；冲突时以本文 +《桌宠游戏 MVP 策划方案》为准，不以临时发挥为准。  
> **仓库**：https://github.com/Maplemaiden/flurry  
> **工程根目录**：本仓库（本地文件夹名可能为 `fluffy`）

---

## 0. 使用方式（给编程代理 / 开发者）

### 0.1 每次开工必须遵守

1. 先确认当前任务属于哪个 **阶段（P1–P4）** 与哪条 **目标编号（Tx.y）**。
2. 只实现该目标列出的 **必须项（MUST）**；**禁止项（MUST NOT）** 一律不做。
3. 完成后用该目标的 **验收标准（Acceptance）** 自检；未写清的交互不要擅自加规则。
4. 改动保持最小范围：不顺手重构无关模块、不新增未要求的依赖、不写无关文档。
5. 提交信息说明「完成了哪条目标 / 为何」，避免空泛的 “update”。

### 0.2 决策优先级（冲突时）

1. **情感原则**（不打扰、不惩罚、碎片化）  
2. **MVP 范围边界**（§8）  
3. **本文目标明细**  
4. **现有代码结构与类型**（`src/shared/types.ts` 等）  
5. 临时审美或“做大一点”的冲动  

### 0.3 产品一句话（所有 UI / 行为的北极星）

> 在屏幕角落，有一团毛茸茸的小生命，它记得你，等着你，用全部的存在轻轻告诉你：「没关系，我在。」

---

## 1. 硬性产品原则（每条都必须可验收）

### T1.1 碎片化的治愈

| 编号 | 要求 | 验收 |
|------|------|------|
| T1.1.1 | 单次主动互动（抚摸、喂食、一句对话）从开始到反馈结束 **≤ 10 秒**（不含用户自己停着看） | 计时或录屏自测通过 |
| T1.1.2 | 不出现强制每日任务、体力耗尽、必须上线才能维持的机制 | 代码与 UI 中无相关系统 |
| T1.1.3 | 不出现倒计时惩罚、错过惩罚、衰减惩罚 | 见 T1.4 |

### T1.2 不打扰的陪伴

| 编号 | 要求 | 验收 |
|------|------|------|
| T1.2.1 | **禁止**系统级打断式通知弹窗作为默认关怀手段（MVP 不做 OS notification 催促） | 无 `Notification` 催促逻辑 |
| T1.2.2 | 桌宠默认安静；主动表演仅在：开机迎接、用户互动后、专注结束、暖心时刻白名单触发 | 行为状态机有明确触发源 |
| T1.2.3 | 「长时间不理睬」时，猫咪应 **降低存在感**（打盹 / 靠边 / 降低动作频率），而不是呼唤用户 | 有 idle→sleep/low-activity 路径 |
| T1.2.4 | 点击穿透模式开启时，不得挡住用户对桌面其他应用的正常点击（穿透区域有效） | Windows/macOS 手动验证 |

### T1.3 双向情感流动

| 编号 | 要求 | 验收 |
|------|------|------|
| T1.3.1 | 用户每次有效照顾/互动，必须有 **即时反馈**（动画 和/或 音效 和/或 气泡文案，至少一种） | 抚摸/喂食/对话均可观测 |
| T1.3.2 | 反馈语义只能是：安慰、鼓励、接纳、亲昵；禁止嘲讽、施压、指责 | 文案词库审查 |
| T1.3.3 | 猫咪可主动表达关心，但频率受控（见 T5.x 暖心时刻） | 有冷却 / 上限 |

### T1.4 不惩罚的温柔（绝对红线）

| 编号 | 要求 | 验收 |
|------|------|------|
| T1.4.1 | **禁止**因久未登录导致生病、死亡、逃跑、亲密度暴跌、房间破败 | 无此类状态与文案 |
| T1.4.2 | 久未互动时，状态只能是：安静等待、沉睡、醒来轻轻迎接 | 状态枚举可映射到这三类 |
| T1.4.3 | 亲密度只允许 **不减或极缓自然漂移（MVP：只增不减）** | `intimacy` 更新路径无减法 |

---

## 2. 技术栈与架构约束

### T2.1 技术选型（MUST）

| 编号 | 要求 |
|------|------|
| T2.1.1 | 桌面壳：**Electron + electron-vite + TypeScript** |
| T2.1.2 | **禁止**引入完整游戏引擎（Unity / Unreal / Godot / Cocos 等）作为运行时 |
| T2.1.3 | 动画层允许：CSS 占位 → 帧动画 / Canvas·Pixi → Spine / Rive（按资源到位再换，不提前绑死） |
| T2.1.4 | 小窝界面保持 Web 渲染（HTML/CSS/TS）；不要为了“更像游戏”改成引擎场景 |
| T2.1.5 | 状态持久化：本地文件（现有 JSON store）；MVP **不做**账号云同步 |
| T2.1.6 | 平台目标：Windows 与 macOS；功能需考虑双边差异，禁止只在一边可跑的硬编码路径（资源路径、托盘图标格式除外可分支） |

### T2.2 进程与窗口架构（MUST）

| 编号 | 要求 | 验收 |
|------|------|------|
| T2.2.1 | **桌宠窗**：无边框、透明、置顶、可拖拽定位、可跳过任务栏 | `petWindow` 配置符合 |
| T2.2.2 | **小窝窗**：独立普通窗口，从托盘或桌宠打开 | `homeWindow` 存在 |
| T2.2.3 | **托盘**：至少提供「打开小窝 / 显隐桌宠 / 退出」 | 菜单三项可用 |
| T2.2.4 | 渲染进程通过 `window.fluffy`（preload）访问主进程；**禁止**在 renderer 开 `nodeIntegration` | `contextIsolation: true` |
| T2.2.5 | 跨窗状态以主进程 store 为唯一真相源，经 IPC 同步 | `GET_STATE` / `SET_STATE` / `STATE_CHANGED` |
| T2.2.6 | 单实例锁：重复启动应聚焦已有实例，不新开多套桌宠 | `requestSingleInstanceLock` |

### T2.3 代码组织（MUST）

| 编号 | 要求 |
|------|------|
| T2.3.1 | 新功能优先落入既有目录：`main/` / `preload/` / `shared/` / `renderer/pet` / `renderer/home` |
| T2.3.2 | 跨进程共享类型放 `src/shared`；IPC 通道名集中在 `channels.ts`，禁止魔法字符串散落 |
| T2.3.3 | 行为状态机、对话词库、暖心触发器应可单测或至少可纯函数化；避免全写在 DOM 事件里 |
| T2.3.4 | 类型：`strict` TypeScript；公共 API 变更必须更新 `types.ts` 与 preload 类型声明 |

### T2.4 性能与资源（MUST）

| 编号 | 要求 | 验收 |
|------|------|------|
| T2.4.1 | 桌宠空闲时 CPU 占用应克制：动画用 CSS/低频率 tick，禁止无意义 `requestAnimationFrame` 空转 | 空闲时无明显风扇/占用尖峰（经验验收） |
| T2.4.2 | 音效默认短、小、可静音；全局 `settings.muted` 必须生效 | 静音后无声 |
| T2.4.3 | 资源未到位时允许占位（CSS 形），但接口要按最终「行为名 / 动画名」设计，避免占位逻辑污染状态机 |

---

## 3. 数据模型目标

### T3.1 `CatProfile`（MVP）

| 编号 | 字段 / 规则 | 要求 |
|------|-------------|------|
| T3.1.1 | `name` | 用户命名；默认可用「Fluffy」；长度建议 1–12；去首尾空格 |
| T3.1.2 | `personality` | MVP **固定一只**可用猫；枚举可保留 `lazy \| playful \| gentle`，但 UI **不提供多选**（结构预留） |
| T3.1.3 | `intimacy` | 整数 **0–100**；MVP 只增不减；单次互动增量小（建议 +1～+3） |
| T3.1.4 | `createdAt` | ISO 时间字符串；领养时写入；只读 |

### T3.2 `AppSettings`

| 编号 | 字段 | 要求 |
|------|------|------|
| T3.2.1 | `opacity` | 0.3–1.0；作用于桌宠可见度 |
| T3.2.2 | `clickThrough` | 布尔；切换桌宠 `setIgnoreMouseEvents` |
| T3.2.3 | `focusMinutes` | 正整数；默认 25；专注计时使用 |
| T3.2.4 | `muted` | 布尔；静音全部猫咪音效与环境音 |

### T3.3 `AppState`

| 编号 | 要求 |
|------|------|
| T3.3.1 | `onboardingDone === false` 或 `cat === null` 时，必须引导领养后才能进入照顾玩法 |
| T3.3.2 | `focusActive` 为专注模式唯一开关；桌宠与小窝必须同时反映 |
| T3.3.3 | 持久化必须在进程退出后可恢复：名字、亲密度、设置、是否完成引导 |

### T3.4 行为枚举 `CatBehavior`（桌宠状态机）

MVP 必须至少支持并可切换：

`idle` | `walk` | `sleep` | `groom` | `yawn` | `pet` | `drag` | `focus` | `celebrate`

| 编号 | 要求 |
|------|------|
| T3.4.1 | 任意时刻有且仅有一个当前行为（可有短时过渡，但对外一个主状态） |
| T3.4.2 | `drag` / `pet` / `focus` / `celebrate` 为高优先级，可打断低频自主行为 |
| T3.4.3 | 禁止未定义行为字符串；扩展必须先改 `types.ts` |

---

## 4. 阶段目标总览

| 阶段 | 名称 | 对应策划 | 完成定义 |
|------|------|----------|----------|
| **P1** | 桌面精灵存在感 | 8.3-1 / 4.2 | 可日常置顶陪伴：动画占位可、抚摸/拖拽/自主行为/显隐可用 |
| **P2** | 小窝与亲密度 | 8.3-2 / 4.1 / 4.3 | 领养 + 喂养/睡觉/陪学 + 亲密度写入 |
| **P3** | 专注、音频、对话、暖心 | 8.3-3 / 4.4 / 4.5 / 5 | 专注可计时结束庆祝；词库对话；开机迎接 |
| **P4** | 旅行与扩展 | 8.3-4 / 4.6 | **非 MVP**；仅接口预留，不实现完整系统 |

**MVP 交付 = P1 + P2 + P3 全部 MUST 完成。**

---

## 5. P1 — 桌面精灵（精细目标）

### T5.1 窗口与显隐

| 编号 | MUST | 验收 |
|------|------|------|
| T5.1.1 | 启动后桌宠出现在主屏工作区内（默认右下角附近） | 视觉确认 |
| T5.1.2 | 置顶且不抢焦点（`showInactive` 或等价） | 打开时不打断当前键鼠焦点 |
| T5.1.3 | 托盘可隐藏/再次显示桌宠 | 菜单项有效 |
| T5.1.4 | 关闭小窝不影响桌宠继续存在 | 关 home 后 pet 仍在 |
| T5.1.5 | 退出只能通过托盘「退出」（或明确退出入口），关窗不等于杀进程（保活策略保持） | 符合现架构 |

### T5.2 拖拽

| 编号 | MUST | 验收 |
|------|------|------|
| T5.2.1 | 按住桌宠可拖到屏幕任意工作区位置 | 松手后位置保持 |
| T5.2.2 | 拖拽中行为为 `drag`；松手回到 `idle`（或短缓冲） | 状态可观测 |
| T5.2.3 | 拖拽不得把窗口拖到完全不可见（建议钳制在 workArea 内） | 边界钳制 |
| T5.2.4 | 拖拽过程避免闪烁/丢指针捕获 | 指针 up 必清理 drag 状态 |

### T5.3 抚摸与点击

| 编号 | MUST | 验收 |
|------|------|------|
| T5.3.1 | 单击或在猫身上划过可触发 `pet` | 有眯眼/呼噜等反馈（占位动画可） |
| T5.3.2 | 抚摸有短时气泡或音效之一（未静音时） | 可感知 |
| T5.3.3 | 双击打开小窝 | `openHome` |
| T5.3.4 | 抚摸成功可微量增加亲密度（若已领养）；未领养不报错 | intimacy +1 级 |

### T5.4 自主行为

| 编号 | MUST | 验收 |
|------|------|------|
| T5.4.1 | 空闲时在 `idle / walk / groom / yawn / sleep` 间低频率切换 | 非每秒乱切；建议间隔数秒～十余秒 |
| T5.4.2 | `walk` 允许小范围移动窗口或角色位移（MVP 可先窗口微移或原地 walk 动画） | 有“活着”的感觉即可 |
| T5.4.3 | 长时间无互动进入 `sleep` 或降低动画幅度 | 符合不打扰 |
| T5.4.4 | 专注模式开启时，自主花活停止，固定 `focus` 安静态 | 不被 yawn/蝴蝶等打断 |

### T5.5 追逐光标 / 随机小动作（P1 可选增强）

| 编号 | 级别 | 要求 |
|------|------|------|
| T5.5.1 | SHOULD | 偶尔朝光标方向看或短距靠近（不可疯狂追导致扰人） |
| T5.5.2 | COULD | 虚拟蝴蝶 / 碰倒杯子等；**必须有冷却**，且专注模式下禁用 |
| T5.5.3 | MUST NOT | 全屏特效、强制置顶弹窗广告式表演 |

### T5.6 透明度与点击穿透

| 编号 | MUST | 验收 |
|------|------|------|
| T5.6.1 | `opacity` 设置能作用到桌宠 | 改设置即生效 |
| T5.6.2 | `clickThrough` 可开关；开启后点击穿透到下方应用 | OS 级验证 |
| T5.6.3 | 若开启穿透，需保留一种唤回交互的方式（托盘或热区方案）；禁止把用户锁死无法点猫 | 有逃生路径 |

---

## 6. P2 — 遇见猫与温暖小窝（精细目标）

### T6.1 初次领养（MVP：1 只）

| 编号 | MUST | 验收 |
|------|------|------|
| T6.1.1 | 未领养时打开小窝显示领养引导，不能空窗 | onboard UI |
| T6.1.2 | 可输入名字并确认领养；写入 `cat` + `onboardingDone=true` | 重启后仍在 |
| T6.1.3 | MVP **不**做多猫外观选择 UI（策划远期有，本阶段禁止扩大范围） | 无多选卡 |
| T6.1.4 | 领养成功后桌宠气泡可用猫名迎接 | 文案含名字 |

### T6.2 小窝入口

| 编号 | MUST | 验收 |
|------|------|------|
| T6.2.1 | 桌宠双击 → 打开小窝 | |
| T6.2.2 | 托盘「打开小窝」→ 打开；若已打开则聚焦 | |
| T6.2.3 | 小窝视觉：温暖、低饱和奶油/焦糖/暖灰；**像插画卡片，不像后台仪表盘** | 无密 stat 墙、无多卡仪表盘首屏 |

### T6.3 喂养

| 编号 | MUST | 验收 |
|------|------|------|
| T6.3.1 | 提供喂养操作（按钮可；目标形态为拖拽食物到猫面前） | 至少一种完整闭环 |
| T6.3.2 | 喂养后播放吃东西/满足反馈 | 动画或文案 |
| T6.3.3 | 喂养增加亲密度（建议 +2） | 数值变化 |
| T6.3.4 | 不设食物库存耗尽惩罚；可无限或极轻限制（若限制只能是节奏，不能惩罚） | 无负面后果 |

### T6.4 睡眠陪伴

| 编号 | MUST | 验收 |
|------|------|------|
| T6.4.1 | 进入 sleep 场景：猫蜷缩/睡觉表现 | `HomeScene = 'sleep'` |
| T6.4.2 | 可播放助眠白噪音（可静音关闭） | 音频开关服从 `muted` |
| T6.4.3 | 用户可随时退出 sleep 回默认 | 一键恢复 |
| T6.4.4 | 不强制用户必须陪睡满多久 | 无失败结算 |

### T6.5 学习陪伴

| 编号 | MUST | 验收 |
|------|------|------|
| T6.5.1 | 进入 study 场景：猫安静趴旁边，动作频率降低 | `HomeScene = 'study'` |
| T6.5.2 | 偶尔抬头看玩家（低频），不发声打扰（或极轻） | 符合安静 |
| T6.5.3 | 可随时退出 | |

### T6.6 基础互动（逗猫棒 / 梳毛）

| 编号 | 级别 | 要求 |
|------|------|------|
| T6.6.1 | SHOULD | 至少一种额外互动（梳毛或逗猫棒），纯表现无复杂规则 |
| T6.6.2 | MUST NOT | 小游戏得分、胜负、失败惩罚 |

### T6.7 亲密度框架

| 编号 | MUST | 验收 |
|------|------|------|
| T6.7.1 | 小窝展示当前亲密度（可朴素文本） | UI 可见 |
| T6.7.2 | 亲密度到达阈值可解锁表现（MVP 至少预留阈值表；可先 1–2 个解锁） | 数据结构存在 |
| T6.7.3 | 解锁内容只能是更亲密的正面动作（露肚皮、举高等），禁止解锁“惩罚解除” | 文案/内容审查 |
| T6.7.4 | 亲密度上限 100；溢出钳制 | `Math.min(100, …)` |

---

## 7. P3 — 对话、专注、暖心时刻（精细目标）

### T7.1 轻量对话（预设词库）

| 编号 | MUST | 验收 |
|------|------|------|
| T7.1.1 | 小窝可输入一句话并得到猫的回复 | 输入→回复闭环 |
| T7.1.2 | 回复来自预设规则/词库，**不是**云端大模型（MVP） | 无外网对话 API |
| T7.1.3 | 至少覆盖情绪类关键词：累/难过/开心/问候等 | 与 `dialogue.ts` 同类扩展 |
| T7.1.4 | 命中规则时回复应温暖具体；未命中用温和兜底（「我在」「咕噜」类） | 无冷漠空回复 |
| T7.1.5 | 特定回复可伴随动画钩子（如累→踩奶）；无资源时允许只改文案+占位态 | 钩子接口预留 |
| T7.1.6 | 对话成功可 +1 亲密度 | |
| T7.1.7 | MUST NOT：医疗/危机干预假装专业建议；若用户表达自伤等，仅温柔陪伴 + 建议寻求现实帮助的短句（可选，不做诊断） | 文案安全 |

### T7.2 专注模式

| 编号 | MUST | 验收 |
|------|------|------|
| T7.2.1 | 可手动开始 / 结束专注 | `toggleFocus` 或 start/stop |
| T7.2.2 | 开始后：`focusActive=true`；桌宠进入 `focus`；停止花活 | 双端一致 |
| T7.2.3 | 使用 `focusMinutes` 倒计时；到时自动结束 | 计时准确（±1s 可接受） |
| T7.2.4 | 专注中播放环境音之一：轻音乐 / 雨声 / 篝火（至少 1 种可切换更佳） | 可听；遵从 muted |
| T7.2.5 | 结束时：`celebrate`（伸懒腰/小花等占位可）+ 短文案祝贺 | 有结束仪式 |
| T7.2.6 | 结束可 +intimacy | |
| T7.2.7 | 专注中禁止：蝴蝶、追逐光标、对话强弹、索要互动 | 状态机门禁 |
| T7.2.8 | MUST NOT：专注失败惩罚、断连续打卡羞辱 | |

### T7.3 暖心时刻（少量、白名单）

| 编号 | MUST | 验收 |
|------|------|------|
| T7.3.1 | **开机 / 应用启动迎接**：随机或轮换 2+ 种欢迎表现（伸懒腰、露肚皮、蹭边等占位） | 每次启动可见一种 |
| T7.3.2 | **专注结束庆祝**：见 T7.2.5 | |
| T7.3.3 | **久坐关怀（可选 SHOULD）**：连续使用达阈值后，桌宠温和气泡/小卡片「已经很久了，抱抱」；**有冷却**（如 ≥ 90 分钟一次） | 不刷屏 |
| T7.3.4 | 暖心表现只在桌宠/小窝内，不抢系统焦点弹窗 | 符合 T1.2 |
| T7.3.5 | 文案语气：软、短、无命令口吻 | 文案表审查 |

### T7.4 音频总则

| 编号 | MUST |
|------|------|
| T7.4.1 | 分类：猫声（呼噜/喵）、互动音、环境音 |
| T7.4.2 | `muted` 关闭所有；后续可分子开关但 MVP 一个总开关即可 |
| T7.4.3 | 音量默认偏低；禁止突然大声惊吓 |

---

## 8. P4 — 非 MVP（禁止在 MVP 中实现，仅允许预留）

| 编号 | 内容 | 规则 |
|------|------|------|
| T8.1 | 多猫选择与完整性格数值体系 | MUST NOT 做完整系统；`personality` 字段可留 |
| T8.2 | 旅行：戴帽出门、现实时间归来、明信片、纪念品图鉴 | MUST NOT 完整实现；若预留，仅空接口/注释 |
| T8.3 | 小游戏、房间装修深度系统、皮肤商店、社区分享 | MUST NOT |
| T8.4 | AI 情绪识别、生成式对话、云端人格记忆 | MUST NOT |
| T8.5 | 账号体系、内购、广告 | MUST NOT |

若用户明确要求提前做 P4，必须先改本文与策划范围，再写代码。

---

## 9. 美术 / UI 实现约束（有资源与无资源时）

### T9.1 风格

| 编号 | MUST |
|------|------|
| T9.1.1 | 色彩：奶油白、焦糖棕、暖灰等低饱和暖色；禁止冷紫赛博默认风、禁止强发光特效堆叠 |
| T9.1.2 | 动画：强调柔软、挤压拉伸；避免机械硬切 |
| T9.1.3 | 小窝：场景化按钮，减少“后台表单”感；首屏不要仪表盘式多模块堆砌 |
| T9.1.4 | 桌宠窗口背景必须透明；不可出现不透明白底方块（除非调试开关） |

### T9.2 无正式美术时

| 编号 | MUST |
|------|------|
| T9.2.1 | 允许 CSS/几何占位猫，但行为名、事件名按正式状态机 |
| T9.2.2 | 占位阶段也要能演示：抚摸、睡觉、专注、庆祝的差异 |
| T9.2.3 | 资源替换时应主要改渲染层，不推翻 IPC/状态 |

---

## 10. 情感反馈实现清单（贯穿 P1–P3）

| 编号 | 机制 | MUST |
|------|------|------|
| T10.1 | 即时反馈 | 一切主动互动 → 1 秒内有可见/可听反馈 |
| T10.2 | 非语言治愈 | 优先动画与音效；文案为辅、宜短 |
| T10.3 | 记忆与成长 | 亲密度驱动解锁；让用户感到“更信任我” |
| T10.4 | 主动关怀 | 仅白名单触发 + 冷却 |
| T10.5 | 安全港 | 永远无惩罚闭环 |

---

## 11. 工程质量目标

| 编号 | MUST |
|------|------|
| T11.1 | `npm run typecheck` 与 `npm run build` 保持通过 |
| T11.2 | 不提交 `node_modules/`、`out/`、密钥、本机绝对路径配置 |
| T11.3 | 新增 IPC 必须：`channels.ts` + preload API + 主进程 handler + 类型 |
| T11.4 | 用户可见文案统一中文（开发注释可中英）；语气符合产品 |
| T11.5 | 错误处理：存储损坏时回退默认状态，不白屏崩溃 |
| T11.6 | 日志：可有开发日志；禁止向用户弹技术堆栈恐吓框 |

---

## 12. 明确禁止的实现习惯

1. 为“更完整”私自加入养成惩罚、饥饿死亡、打卡羞辱。  
2. 用系统通知轰炸“你的猫想你了”。  
3. 引入游戏引擎或过重 3D 运行时。  
4. 把小窝做成数据后台（大量数字、任务列表、成就墙首屏）。  
5. 未立项就接 OpenAI 等云端对话。  
6. 在 renderer 直接 `require('fs')` 写盘。  
7. 亲密度用浮点乱涨或一次互动 +20 以上（破坏成长感）。  
8. 专注模式做成番茄强迫症：失败扣分、断连惩罚。

---

## 13. 推荐实现顺序（执行清单）

按序勾选；未完成前一项的关键验收，不进入下一项的大功能。

- [ ] **P1.A** 桌宠窗稳定：置顶、透明、托盘显隐、单实例  
- [ ] **P1.B** 拖拽 + 边界钳制 + drag 状态  
- [ ] **P1.C** 抚摸反馈 + 双击开窝  
- [ ] **P1.D** 自主行为状态机（含久置睡眠）  
- [ ] **P1.E** opacity / clickThrough 设置生效  
- [ ] **P2.A** 领养引导与持久化  
- [ ] **P2.B** 喂养闭环 + intimacy  
- [ ] **P2.C** sleep / study 场景  
- [ ] **P2.D** 亲密度展示与阈值解锁表（可先 1 档）  
- [ ] **P3.A** 词库对话 + 动画钩子接口  
- [ ] **P3.B** 专注计时 + 环境音 + 结束庆祝  
- [ ] **P3.C** 启动迎接 + 暖心冷却触发  
- [ ] **P3.D** 静音与音效分层整理  
- [ ] **MVP 打磨** 文案、性能、Win/Mac 冒烟  
- [ ] **P4** 仅文档/接口预留，不进 MVP 包

---

## 14. 验收总闸（MVP Done）

同时满足以下才可称 MVP 完成：

1. 用户能领养一只猫并长期本地记住。  
2. 桌宠可日常置顶陪伴：拖拽、抚摸、自主闲置/睡觉，且不打扰工作。  
3. 小窝可喂养、陪睡、陪学，并看到亲密度增长。  
4. 专注模式可开始、安静陪伴、到时庆祝。  
5. 可对猫说话并得到温暖预设回复。  
6. 有启动迎接；无任何惩罚养成。  
7. 无引擎依赖；`typecheck` / `build` 通过。

---

## 15. 附录：与现有骨架的映射

| 区域 | 路径 | 后续主要改这里 |
|------|------|----------------|
| 桌宠表现 / 状态机 | `src/renderer/pet/` | P1、P3 迎接/专注态 |
| 小窝 / 领养 / 照顾 | `src/renderer/home/` | P2、P3 对话与专注 UI |
| 对话词库 | `src/renderer/home/dialogue.ts` | P3 扩展规则与钩子 |
| 窗口 | `src/main/windows/` | 穿透、边界、多屏 |
| 托盘 | `src/main/tray.ts` | 菜单与图标 |
| IPC | `src/main/ipc.ts` + `preload` | 新 API |
| 持久化 | `src/main/store.ts` | 字段扩展 |
| 类型 | `src/shared/types.ts` | 一切模型变更的源头 |

---

**结语**：每一个功能的添加强度都要过问——它是否更「不打扰」？是否更「毛茸茸」？是否让人更安心？若否，默认不做。
